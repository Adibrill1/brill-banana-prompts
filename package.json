{
  "name": "brill-banana-prompts",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "tsc --noEmit && vite build && node scripts/prepare-images.mjs && node scripts/prepare-catalog.mjs && node scripts/copy-public.mjs",
    "preview": "node scripts/dev.mjs --production",
    "test": "node --test tests/*.test.cjs"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.0"
  },
  "engines": {
    "node": "22.x"
  }
}
